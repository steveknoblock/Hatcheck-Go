"+ New Collection" sits in the header next to "+ New" and "↓ Import", as you confirmed. Clicking it replaces the whole editor/relations area with a dedicated builder — deliberately not a third tab, since Editor and Relations are both views onto whatever object is currently open, while building a Collection is composing several other objects into a brand new one. Trying to force it into the same tab-strip would've been the wrong shape for what it actually is.

The Import feature accepts a compressed file of objects and brings them into the current workspace.

The Import button sits above the content editing area.

The Editor and Relations tabs are views into the object currently open for editing.

A Collection composes several objects into new one?


## About Namespaces and Names

Names and namespaces are *metadata* about objects. A collection is a list of object identifiers without respect to names or name paths. At the storage layer there is no requirement for collections to respect namespace boundaries.


## Collection Display

The collection display in the editor seems confusing. Initially, it shows a list of objects in the collection with links to show the particular object in the editor when clicked on. Once clicking on the link, the collection disappears from the list and the list only shows the objects in the collection. The name no longer shows in the side bar listing but moves to the namespace path above the object in the editor.


